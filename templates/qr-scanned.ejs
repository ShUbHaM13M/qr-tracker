<!DOCTYPE html>
<html lang="en">
  <head>
    <title></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/css/styles.css" rel="stylesheet">
  </head>
  <body>
    <main>
      <h1>A QR just got scanned</h1>

      <hr />

      <% switch (type) {
        
        case 'text' : %>
          <p><%= text %></p>
        <% break;

        case 'image' : %>
          <img src="<%= text %>" alt="QR image" />
        <% break;

        case 'link' : %>
          <a href="<%= text %>"><%= text %></a>
        <% break;

      } %>
    </main>
  </body>
</html>

