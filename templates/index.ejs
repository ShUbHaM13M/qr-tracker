<!DOCTYPE html>
<html lang="en">
  <head>
    <title></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/css/styles.css" rel="stylesheet">
  </head>
  <body>
    <main>
      <h1>QR code generator</h1>
      <hr />
      <form class="form" action="/" method="get">
        <label for="text">Enter text to generate QR</label>
        <div>
          <input id="text" type="text" name="text" value="" placeholder="Hello World!" />
          <button title="Generate" type="submit">Generate</button>
        </div>
      </form>
      <% if (qrDataURL) { %>
      <img id="qrImage" src="<%= qrDataURL %>" alt="QrCode" />
      <% } %>
    </main>
  </body>
</html>

